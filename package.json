{
  "name": "operation-ghost-reboot",
  "version": "1.0.0",
  "description": "Operation: Ghost Reboot - Browser FPS",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.158.0"
  },
  "devDependencies": {
    "@types/three": "^0.158.0",
    "typescript": "^5.3.2",
    "vite": "^5.0.0"
  }
}
